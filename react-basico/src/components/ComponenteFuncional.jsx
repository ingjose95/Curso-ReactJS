import React from 'react';

function ComponenteFuncional( { msg }) {

    return (

        <h1>{msg}</h1>


    )
}

// const ComponenteFuncional = (props) => <h1>{props.msg}</h1>

export default ComponenteFuncional;